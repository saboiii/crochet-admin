import Layout from "@/components/layout/Layout";
import Head from "next/head";

export default function Orders() {
    return (
        <Layout>
            <Head>
                <title>Crochets | Orders</title>
                <meta name="description" content="Admin orders page for Crochet E-Commerce." />
            </Head>
            <div>Orders page.</div>
        </Layout>
    );
}